<Window x:Class="AutoBuild.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodel="clr-namespace:AutoBuild.ViewsModels"
        xmlns:local="clr-namespace:AutoBuild.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources >
        <viewmodel:MainWindowViewModel x:Key ="MainWindowViewModel" /> 
    </Window.Resources >
    <Grid DataContext = "{Binding Source={ StaticResource MainWindowViewModel}}">
        <Grid.RowDefinitions>
            <RowDefinition Height = " Auto "/>
            <RowDefinition Height = " 3 * "/>
        </Grid.RowDefinitions >
        <Button 
            Content="Obtener" 
            Margin="10" 
            HorizontalAlignment="Left" 
            Command="{Binding MockUserCommand}"
                />
        <DataGrid ItemsSource="{Binding MockUsers}" Grid.Row="1" Margin="10" />
    </Grid>
</Window>
